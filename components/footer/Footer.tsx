import * as React from "react"

export interface FooterProps {}

export default function Footer(props: FooterProps) {
  return (
    <footer className="w-screen h-screen flex items-center justify-center">
      <nav></nav>
      <div></div>
    </footer>
  )
}
